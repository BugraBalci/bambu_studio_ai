# Placeholder for Phase B — export P2S machine/process/filament presets from Bambu Studio here.
# Recommendation `slicer_hints` keys map onto these JSON profiles for CLI slicing.
#
# bambu_project_settings_template.json — base blob for project .3mf export (patched to P2S at runtime).
